# water (WebAssembly TExt foRmat compiler)

`water` aims to be a tiny and performant WebAssembly Text Format compiler.

## Version Control

The image displays a Git branching icon consisting of three circular nodes connected by curved lines, forming a branching structure. This icon represents Git's version control capabilities, illustrating how code branches diverge and can be merged in a distributed version control system.

